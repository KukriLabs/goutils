package sqlxutils_test

import (
	"testing"
)

func TestStmtPreparer(t *testing.T) {
	tt := []struct {
		name string
	}{
		{name: ""},
	}
	for _, tc := range tt {
		t.Run(tc.name, func(t *testing.T) {
		})
	}
}

func TestNameStmtPreparer(t *testing.T) {
	tt := []struct {
		name string
	}{
		{name: ""},
	}
	for _, tc := range tt {
		t.Run(tc.name, func(t *testing.T) {
		})
	}
}
